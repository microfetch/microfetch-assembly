image: docker:19.03.12-dind
variables:
  DEBIAN_FRONTEND: noninteractive

stages:
  - test

testing:
  stage: test
  script:
    - echo $PWD
    - touch $PWD/hello.txt
    - ls -la
    - docker run --privileged -w $PWD -v $PWD:$PWD quay.io/nextflow/bash bash -c 'ls -la small_test_input'

